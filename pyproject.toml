[project]
name = "footballai-monorepo"
version = "0.1.0"
description = "FootballAI Turbo + uv monorepo."
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
